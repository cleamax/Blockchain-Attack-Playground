name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js (for linters)
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1
        with:
          version: nightly

      # 🔧 Make forge install reliable in Actions
      - name: Prepare workspace for forge
        run: |
          mkdir -p lib
          git config --global user.email "actions@github.com"
          git config --global user.name "GitHub Actions"
          git config --global --add safe.directory "$GITHUB_WORKSPACE"

      # 📦 Install forge-std and build
      - name: Install Foundry deps & build
        run: |
          forge --version
          # install only if not already present
          if [ ! -d lib/forge-std ]; then
            forge install --no-commit foundry-rs/forge-std
          fi
          forge build

      # 🔎 Linters
      - name: Install linters
        run: |
          npm install -g solhint prettier prettier-plugin-solidity

      - name: Run solhint
        run: solhint 'challenges/**/*.sol'

      - name: Run prettier check
        run: prettier --check 'challenges/**/*.sol'

      # ✅ Tests
      - name: Run tests
        run: forge test -vv

      # 📈 Coverage
      - name: Run coverage
        run: forge coverage --report lcov

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: lcov.info
          path: lcov.info
          if-no-files-found: error

          name: lcov.info
          path: lcov.info
          if-no-files-found: error
